# leafree マニュアル

Ｅｘｃｅｌが表による計算ソフトなら、leafreeは木による計算ソフトです。

ノード（Excelのセルに対応）が木構造で階層的に連結しています。

~~各ノードの表示箇所は、原則、採用している[ツールd3.jsのTree](https://d3js.org/d3-hierarchy/tree)に依存していますが、ドラッグすることにより、好きな場所に移動できます。~~

各ノードは、親子関係・兄弟関係に基づいて縦・横の間隔などを計算した位置に表示します。ノード毎に縦・横の間隔を指定することができます。ノードを特定の位置に表示することもできます（[表示位置の移動](# 表示位置の移動)、[ルートノードへの子ノードの追加](# ルートノードへの子ノードの追加)）。

データはXML形式で保存・読込ができます。



## インストール・起動方法

ダウンロードした`leafree.html`と`leafree_tree.js`を、同じフォルダに保存します。

`leafree.html`をダブルクリックすると、ブラウザソフト上で、プログラムが起動します。または、ブラウザソフトのウィンドゥ上に、`leafree.html`をドラッグします。

※`Firefox`など一部のブラウザでは、使えない機能があります。`GoogleChrome`か、`MicrosoftEdge`の利用を推奨します。

## 画面表示

### ボタン

#### XMLで保存

#### XMLファイルの読み込み

### ノード

ノードは、leefreeで管理するデータの最小単位です。Excelのセルに対応します。

ノードは、ノード名とノード内容と様々な属性（フォント名、表示形式など）が含まれています。

[ノード内容](# ノード内容)は、数値（整数、小数）、文字列、計算式です。計算式の中で`=10*'個数'`のように他のノードをノード名（個数）で指定することができます。

ノードを示す四角形の中にノード内容（計算式は計算結果）を指定されたフォント、色、表示形式で表示します。

四角形の上にはノード名、下にはノード内容を表示します。

フォント名や色などは、ノード毎に指定することもできますし、デフォルト指定もできます。

ノードにマウスポインタを置くと、ノードに「メモ」属性として設定された内容が、吹き出し（ツールチップ）として表示されます。シフトキーを押しながらマウスポインタを置くと、ノードの計算途中の式が、吹き出し（ツールチップ）として表示されます。

全ノードの中に、常に、1つだけ起点・中心となる[ルートノード](# ルートノード)があります。

### ツリー

#### 親子関係

ノードは複数の子ノードを持つことができます。子ノードを持たないノードもあります。

[ルートノード](# ルートノード)以外は、必ず親ノードを持ちます。

ルートノード以外のノードから、親ノードを順々に辿っていくとルートノードに辿り着きます。

ルートノード以外のノードは、ルートノードの子孫ノードと言えます。

通常、子ノードは親ノードの右、少し下に表示されます。

親子関係は、ノード間を曲線で結んで表示します。親ノードの四角形の右端と子ノードの四角形の左端を結んでいます。

#### 兄弟関係

親ノードが同じノード同士は、兄弟ノードと言います。

通常は、弟ノードは兄ノードの下に表示されます。

#### ノードの計算式の参照

計算式に`=10*'個数'`のように他のノードをノード名（個数）で指定することができます。

このとき、そのノード（参照元ノード）から、個数ノード（参照先ノード）に向かって、直線の矢印を表示します。参照元ノードの中心と参照先ノードの左端を結びます。

#### 位置



## 表示内容・操作方法

ノードにノード名とノード内容（数値、文字列、式）を設定すると、ノードを示す四角に計算結果を表示します。四角の上にノード名、下にノード内容を表示します。

親ノードから子ノードへは曲線の矢印を表示します。

式で他のノードを参照しているときは、直線の点線の矢印を表示します。

### 表示内容

ノードの計算結果~~、内容、ノード名　未対応~~の文字や線の表示は、指定できます。

ツリー全体の指定、ノード毎の指定ができます。ノード毎の指定が優先されます。

どちらの指定もなければ、システムで定めた指定になります。

#### フォントの指定

フォント名、フォントサイズを指定できます。



### 主な操作

#### 子ノードの追加

ノード（の四角）の上で、**shiftキーを押しながらダブルクリック**して、開いた画面にノード名・ノード内容を入力します。

または、ノード（の四角）の上で、右クリックするとメニューが表示されるので、「子ノード挿入」を選択し、開いた画面にノード名・ノード内容を入力します。

ノードの内容を入力時に、==他のノードをクリック==すると、==クリックしたノード名がシングルクォーテーション付き（ノード名直接指定）で入力==されます。



#### ルートノードへの子ノードの追加

**何もないところでダブルクリック**し、開いた画面にノード名・ノード内容を入力します。

ノードの内容を入力時に、==他のノードをクリック==すると、==クリックしたノード名がシングルクォーテーション付き（ノード名直接指定）で入力==されます。

**ダブルクリックした場所**に、ルートノードの子ノードが表示されます。

[上記の「子ノードの追加」](# 子ノードの追加)でも、ルートノードに子ノードを追加できます。

#### ノードの変更

ノード（の四角）の上で、**（shiftキーを押さずに）ダブルクリック**して、開いた画面にノード名・ノード内容を入力します。

または、ノード（の四角）の上で、右クリックするとメニューが表示されるので、「名前・式変更」を選択し、開いた画面にノード名・ノード内容を入力します。

ノードの内容を入力時に、==他のノードをクリック==すると、==クリックしたノード名がシングルクォーテーション付き（ノード名直接指定）で入力==されます。

#### ノードの削除

ノード（の四角）の上で、右クリックするとメニューが表示されるので、「削除」を選択します。

ルートノードは削除できません。

**子孫ノードも一緒に削除**されますので、子孫ノードがあるときは、キャンセルするかの確認の画面が表示されます。

#### ノードの表示位置の移動

ノード（の四角）の上で、**右クリックしたままドラッグ**します。

移動したい先でドロップ（右ボタンを離す）します。

子孫ノードも一緒に移動します。

※ドロップした所が、他のノードの上だと、[下記の「親ノードの変更」](# 親ノードの変更)になります。

#### 親ノードの変更

ノード（の四角）の上で、**右クリックしたままドラッグ**します。

**新しい親ノードの上**でドロップ（右ボタンを離す）します。

※ドロップした所が、何もないところだと、[上記の「ノードの表示位置の移動」](# ノードの表示位置の移動)になります。

#### ノードのコピー・ペースト

ノード（の四角）の上で、右クリックするとメニューが表示されるので、「コピー」を選択します。

ノード（の四角）の上で、右クリックするとメニューが表示されるので、「子としてペースト」を選択すると、**コピーされたノード及び子孫ノード**が、子ノードとして追加されます。

#### ノードの表示位置リセット

ノードの表示位置は、[ツールd3.jsのTree](https://d3js.org/d3-hierarchy/tree)というシステムにより決まります。

ただし、[ノードをドラッグして移動した場合](# ノードの表示位置の移動)、[ルートノードへの子ノードの追加](# ルートノードへの子ノードの追加)の場合は、固定した位置にノードを表示します。

この固定した位置への表示を取り止め、表示位置をシステムに任す場合は、下記の操作をします。

ノード（の四角）の上で、右クリックするとメニューが表示されるので、、「位置リセット」をクリックします。

#### 文字の字体（フォントの種類）

ノード（の四角）の上で、右クリックするとメニューが表示されるので、、「ノード設定」をクリックすると、子メニューが表示されるので「フォントの種類」を選択します。

開いた画面に、`MS Mincho`,`ＭＳ Ｐゴシック`のようにフォント名を指定します。

「デフォルト設定」により「フォントの種類」を選択すると、ノード設定でフォントの種類を設定したノード以外のノード全部のフォントの種類を変えることができます。

#### 文字の大きさ（フォントサイズ）

ノード（の四角）の上で、右クリックするとメニューが表示されるので、、「ノード設定」をクリックすると、子メニューが表示されるので「フォントサイズ」を選択します。

開いた画面に、`24px`、`12pt`,`large`のように文字の大きさを指定します。

「デフォルト設定」により「フォントサイズ」を選択すると、ノード設定で文字の大きさを設定したノード以外のノード全部の文字の大きさを変えることができます。

#### 文字の色

ノード（の四角）の上で、右クリックするとメニューが表示されるので、、「ノード設定」をクリックすると、子メニューが表示されるので「色」を選択します。

開いた画面で、文字の表示に使いたい色を選択します。色の指定方法はブラウザにより、異なります。「ＯＫ」ボタンがないときは、他の何もない場所をクリックします。

「デフォルト設定」により「色」を選択すると、ノード設定で色を設定したノード以外のノード全部の文字の色を変えることができます。

#### 表示形式（フォーマット）

表示形式を指定することにより、カンマ区切りや小数点以下の表示方法などを指定することができます。

ノード（の四角）の上で、右クリックするとメニューが表示されるので、、「ノード設定」をクリックすると、子メニューが表示されるので「表示形式」を選択します。

開いた画面に、`,d`、`.2f`のように表示形式を指定します。

表示形式は、https://observablehq.com/@d3/d3-formatなどを参照してください。

「デフォルト設定」により「表示形式」を選択すると、ノード設定で表示形式を設定したノード以外のノード全部の表示形式を変えることができます。

#### メモ編集

マウスをノードの上に重ねると、メモが吹き出し（ツールチップ）で表示されます。

ノード（の四角）の上で、右クリックするとメニューが表示されるので、、「ノード設定」をクリックすると、子メニューが表示されるので「メモ編集」を選択します。

開いた画面に、メモを入力します。

なお、シフトキーを押しながら、マウスをノードの上に重ねると、そのノードの計算途中の式が吹き出し（ツールチップ）に表示されます。

#### 相対的な表示位置指定



[ノードの表示位置の移動（絶対的な表示位置指定）](# ノードの表示位置の移動)





## データ構造

### ノード

データを管理する最低単位です。Excelにおけるセルのようなものです。

ノードには、ノード名とノード内容（数値・文字列・式）を設定します。

将来的には、書式・色・フォントや幅・高さなどの設定も行います。

### 親子関係・兄弟関係

ルートノード以外のノードには必ず一つの親ノードがあります。

ノードは、複数の子ノードを持つことができます。子ノードがない場合、子ノードが１つだけの場合もあります。

親ノードが同じ子ノード同士は、兄弟ノードといいます。

```
root －－－→ A　－┬－－→　B
　　　　　　　　　　└－－→　C
```

root の子ノードは A。

A の親ノードは root、子ノードは BとC。

B の親ノードは A、子ノードはなし。

C の親ノードは A、子ノードはなし。

BとCは、兄弟ノード。

### ルートノード

ルートノードは１つだけあります。

ルートノードには親ノードがありません。

親ノードには、データ全体の設定データがあります。

### ノード名

`"`,`'`,`/`,`*`,`.`,`&`,`<`,`>`,` `（半角空白）~~,全角の数字~~は、ノード名には使えません。　

~~ノード名は、数字（半角・全角）、XML（大文字・小文字）で始まってはいけない。~~

~~記号（!#$%&など）は、ノード名に使わないことを薦めます（エラーとはなりませんが、将来使えなくなる可能性があります）。~~



兄弟ノードでは同じノード名は使えません（無名ノードを除く）。

親子間などでは同じノード名を使うことができます。

ノード名の最初の文字が`_`のノードは、「無名ノード」と呼び、特別な取扱いになります。

#### 無名ノード

兄弟ノードでも、ノード名の最初の文字が`_`である無名ノードだけは、同一のノード名が使用できます。

無名ノードのノード名の例: `_`,`_相続人`

### ノード内容

ノードには、数値（整数、小数）、文字列、計算式を入力できます。

文字列の場合は、先頭に`'`を付けるか、前後を'"'で括ります。

計算式で他のノードの値を参照するときは、[ノードを指定](# ノード指定)します。

#### 計算式

先頭に、`=`を付けます。

例えば、`=12+3*2 - 8/4`と入力すると、ノードには`16`が表示されます。

`='価格' / '面積'`と入力し、価格という名前のノードの値が`1,200,000`、面積という名前のノードの値が`1,000`なら、ノードには、`1,200`が表示されます。

#### 関数

`max`,`sqrt`,`sum`など、様々な関数が使えます。

文字列を結合する`concat`なども使えます。

[math.jsのevaluate機能](https://mathjs.org/docs/reference/functions/evaluate.html)を利用していますので、[関数等](https://mathjs.org/docs/expressions/syntax.html)が使えます。

#### ノード指定

計算式の中で、他のノードを指定すると、そのセルの値を使って計算します。

ノードの指定方法には、[ノード名直接指定](# ノード名直接指定)と[ノードパス指定](# ノードパス指定)があります。

※ノードの追加、削除、ノード名の変更により、指定先のノードが変わることがあります。

##### ノード名直接指定

ノード名をシングルクォーテーション（`'`）で囲むことにより、指定します。

同じノード名がある場合は、下記の探索順序で参照先のノードを特定します。

複数のノードが（同じ探索タイミングで）見つかった場合は、配列として処理します~~エラーになります~~。

例えば、`=sum('孫')`の場合に、孫というノード名のノードが３個あり、それぞれのノードの内容が、2,3,7だった場合は、`=sum([2,3,7])`と扱い、最終的には`12`になります。

`='孫'+3`の場合は、`[5, 6, 10]`となります。

###### 探索順序

１．まずは、子孫ノードの探索を行います。

　１－１．最初に、子ノードから探します。

　１－２．子ノードになければ、孫ノードから探します。

　１－３．孫ノードになければ孫ノードの子ノード、さらに子ノードと順番に探します。

２．子孫ノードになければ、祖先ノードの探索を行います。

　２－１．親ノードから探します（親ノードは1つなので、ノード名が一致するか）。

　　２－１－１．親ノードになければ、親ノードのの子ノード（兄弟ノードになります）から探します。

　　２－１－２．親ノードの子ノードになければ、親ノードの孫ノード、さらに子ノード、…と順番に探します。

　２－２．親ノード・親ノードの子孫ノードになければ、親ノードの親ノードから探します。

　　２－２－１．親ノードの親ノードになければ、親ノードの親ノードの子ノード、さらに孫ノード、…と順番に探します。

　２－３．親ノードの親ノード・親ノードの親ノードの子孫ノードになければ、親ノードの親ノードの親ノードから探します。

　　２－３－１．親ノードの親ノードの親ノードになければ、親ノードの親ノードの親ノードの子ノード、さらに孫ノード、…と順番に探します。

　２－４．同じように、さらに親ノード、その子孫ノードの順番に探します。

##### ノードパス指定

親子関係を前提とした次のような指定方法もあります。

`sum('*')`のように使うことを想定しています。

`'*'`	子ノードを全部指定します。

`'**'`	子孫ノードを全部指定します。

`'*./child'` -> `./child`	子ノードのうちノード名が`name`のものを指定します。現実は複数になることはないので、`child`と指定するのと同じことになります。

`'*.//name'` -> `.//name`	子孫ノードのうちノード名が`name`のものを指定します。

`'*./child/grandchild'`	ノード名が`child`の子ノードの子ノードのうちノード名が`grandchild` のものを指定します。現実には複数になることはない。

`'*./child//name'`	ノード名が`child`の子ノードの子孫ノードのうちノード名が`name`のものを指定する。　孫ノードに限定していない、`//`である点に注意

`'*.//node//name'`		ノード名が`node`の子孫ノードの子孫ノードのうちノード名が`name`のものを指定する。

`***//name` -> `//name`	ルートノードの子孫ノードのうちノード名が`name`のものを指定する。

`***/child` -> `/child`	ルートノードの子ノードのうちノード名が`child`のものを指定する。



`/`,`//`を繰り返すことや、最後に`*`,`**`を付けることも可能。

`*./child//name/*` -> `./child//name/*`	ノード名が`child`の子ノードの子孫ノードのうちノード名が`name`のものの子ノードを全部指定する。



==要注意== パスの途中で指定されたノード名のノードの削除・追加・ノード名変更により、指定されたノードが変わることがあるが、jsono版では未対応。==xml版で対応予定だが、未対応==

#### エラー

計算式の誤りなどでエラーが発生した場合は、ノードに、`#`から始まるエラーメッセージが表示されます。

`#LOOP?`	参照するノードが循環しています。

`#EMPTY?`	参照したノードの内容が空です。

`#NAME_NOTOFOUND`	指定されたノード名のノードが見つかりません。

`#DUPLICATE?`	指定されたノード名のノードが複数見つかりました。

`#INVALID?`	ノード名などの指定が間違っています。

\#INVALID_NODE_PATH	ノードパスの指定が間違っています。

## ファイル

データは、jsonファイルの形式にて保存、読込をします。

将来的には、xmlファイルに変更予定です。

